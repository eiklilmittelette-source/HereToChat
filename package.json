{
  "name": "heretochat",
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install && npx vite build",
    "start": "cd server && node index.js"
  }
}
